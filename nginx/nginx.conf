server {
    listen 80;

    root /home/ec2-user/register-app/build;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html;
    }

    error_page 404 /index.html;
}
